# Examples

- **`minimal_guard/`** — tiny JSON + YAML for an offline `sentinel guard check` run (no API keys).
- **`fixtures/contract_check/`** — schemas, prompts, and regression suite inputs used by tests and documentation.
