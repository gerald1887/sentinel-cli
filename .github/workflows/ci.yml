name: CI

on:
  push:
    branches:
      - main
      - "release/**"
  pull_request:
    branches:
      - main

jobs:
  test:
    name: "Test (Python ${{ matrix.python-version }})"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package and dev dependencies
        run: pip install -e ".[dev]"

      - name: Run tests (excluding packaging tests)
        run: |
          pytest -q \
            --ignore=tests/integration/test_packaging_install_smoke.py \
            --ignore=tests/integration/test_packaging_help_stability.py \
            --ignore=tests/integration/test_packaging_cli_parity.py \
            --ignore=tests/integration/test_packaging_reproducible_install.py

  packaging:
    name: "Packaging tests (Python 3.11)"
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install package and dev dependencies
        run: pip install -e ".[dev]"

      - name: Run packaging tests
        run: |
          pytest -q \
            tests/integration/test_packaging_install_smoke.py \
            tests/integration/test_packaging_help_stability.py \
            tests/integration/test_packaging_cli_parity.py \
            tests/integration/test_packaging_reproducible_install.py
